# URL of the backend API/Socket.IO server.
# Leave unset in local dev to auto-derive from the page's own hostname (http://<host>:3001).
# Must be set explicitly for any production build.
REACT_APP_BACKEND_URL=https://api.example.com
